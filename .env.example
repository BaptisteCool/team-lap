VITE_CONVEX_URL=your-convex-url-here

# Convex environment variable — NOT stored in .env.local, managed directly in Convex.
# Configure via CLI: npx convex env set SUPER_ADMIN_PIN <value>
# Used by events.create mutation and validateSuperAdminPin query to gate super-admin actions.
SUPER_ADMIN_PIN=